# --- HMMAF Live Test Stack ---
# Pinned to versions known to work on Python 3.11 / CPU-only.

# Communication
redis==5.0.7
fakeredis==2.23.5

# Reasoning Engine (LiteLLM client for OpenAI-compatible LLM at 192.168.1.168:4000)
litellm==1.59.0
openai>=1.40.0

# Perception (Ultralytics YOLO26 + dependencies)
ultralytics>=8.3.0
opencv-python-headless==4.10.0.84
numpy>=1.26,<2
torch==2.3.1
torchvision==0.18.1
Pillow>=10.0.0

# Expression - TTS (Supertonic2 ONNX) + STT (faster-whisper)
supertonic==1.3.1
onnxruntime>=1.18.1
soundfile==0.12.1
huggingface_hub>=0.24.0
faster-whisper==1.0.3
edge-tts==6.1.12

# UI
gradio>=5.50.0,<6

# Testing & utilities
pytest==8.3.2
pytest-asyncio==0.23.8
psutil
requests
python-dotenv
